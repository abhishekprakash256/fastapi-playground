[project]
name = "fastapi-playground"
version = "0.1.0"
description = "repo to test the fast api"
readme = "README.md"
requires-python = ">=3.10.0"
dependencies = [
    "auth0-fastapi-api>=1.0.0b6",
    "fastapi[standard]>=0.135.1",
    "pydantic-settings>=2.13.1",
]
